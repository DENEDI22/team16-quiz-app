import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-agb',
  standalone: true,
  imports: [RouterLink],
  templateUrl: './agb.html',
  styleUrl: './agb.scss',
})
export class Agb {}
