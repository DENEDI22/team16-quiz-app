apiVersion: apps/v1
kind: Deployment
metadata:
  name: scoreboard-service
  namespace: quiz-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: scoreboard-service
  template:
    metadata:
      labels:
        app: scoreboard-service
    spec:
      containers:
        - name: scoreboard-service
          image: quiz-app-scoreboard-service
          ports:
            - containerPort: 3000
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: quiz-app-secrets
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: quiz-app-secrets
                  key: POSTGRES_PASSWORD
            - name: DATABASE_URL
              value: postgres://$(POSTGRES_USER):$(POSTGRES_PASSWORD)@postgres:5432/scoreboard-service
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: quiz-app-secrets
                  key: JWT_SECRET
          readinessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 3
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: scoreboard-service
  namespace: quiz-app
spec:
  selector:
    app: scoreboard-service
  ports:
    - port: 3000
