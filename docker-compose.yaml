services:
  db-quiz-app:
    image: "postgres:latest"
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: Start123!
      POSTGRES_DB: default_database
    volumes:
      - ./db-data/:/var/lib/postgresql/

<<<<<<< HEAD
  auth:
    build:
      context: ./backend/
      dockerfile: ./crates/auth-service/dockerfile
    environment:
      DATABASE_URL: postgres://admin:Start123!@db-quiz-app:5432/quiz-auth
      JWT_SECRET: dev-only-secret
    ports:
      - "3000:3000"
    depends_on:
      - db-quiz-app

volumes:
  pgdata:
=======
  db-quiz-service:
    image: "postgres:latest"
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: Start123!
      POSTGRES_DB: quiz-service
    volumes:
      - ./db-data/:/var/lib/postgresql/
>>>>>>> 9b27728 (-Moved config to .env)
